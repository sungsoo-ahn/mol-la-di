# ZINC250k VAE Full Training Config

output_dir: "data/runs/zinc250k_vae"

seed: 42

data:
  root: "data/datasets"
  dataset: "zinc250k"
  max_atoms: 38
  num_workers: 4

model:
  num_atom_types: 10  # X, C, N, O, F, S, Cl, Br, I, P
  num_bond_types: 5
  d_model: 256
  d_latent: 64
  nhead: 8
  encoder_layers: 4
  decoder_layers: 4
  dim_feedforward: 1024
  dropout: 0.1

training:
  epochs: 300
  batch_size: 128
  eval_batch_size: 256
  lr: 3.0e-4
  min_lr: 1.0e-6
  weight_decay: 0.01
  max_grad_norm: 1.0
  warmup_epochs: 10
  beta_warmup_epochs: 50
  edge_weight: 1.0
  use_scheduler: true
  save_every: 20
  eval_every: 20

eval:
  n_samples: 1000
  final_n_samples: 10000
  temperature: 1.0

wandb:
  enabled: true
  project: "molecule-vae"
  name: "zinc250k_vae"
